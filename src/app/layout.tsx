import type { Metadata } from "next";
import localFont from "next/font/local";
import "./globals.css";

const geistSans = localFont({
  src: "./fonts/GeistVF.woff",
  variable: "--font-geist-sans",
  weight: "100 900",
});

const geistMono = localFont({
  src: "./fonts/GeistMonoVF.woff",
  variable: "--font-geist-mono",
  weight: "100 900",
});

export const metadata: Metadata = {
  title: "Lumora — Understand Your Data in Seconds",
  description:
    "Upload a dataset. Understand what it means. Story-first data understanding for everyone.",
  keywords: ["data analysis", "data understanding", "CSV", "insights", "analytics"],
  authors: [{ name: "Lumora" }],
  openGraph: {
    title: "Lumora — Understand Your Data in Seconds",
    description: "Upload a dataset. Understand what it means.",
    type: "website",
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en" className="light">
      <body
        className={`${geistSans.variable} ${geistMono.variable} font-sans antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
